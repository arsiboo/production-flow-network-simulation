from base_controller import BaseController

# Here I define the controller class which inherits from the base controller, it is the same as the base controller class, but it keeps the code cleaner and allows future extensions
class SimulationController(BaseController):
    pass